# Claude Code — verification & test prompt

Paste the block below into Claude Code (run from the repo root). It checks the
scaffold, sets up the environment, runs the tests, validates Docker, and produces
a gap report. The optional final section lets it implement the remaining phases.
